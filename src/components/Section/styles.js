import {StyleSheet} from 'react-native';
import colors from '../../constants/colors';

export default StyleSheet.create({
  container: {marginBottom: 16, backgroundColor: colors.white},
  titleBox: {padding: 16},
  title: {color: colors.shopee_orange},
});
